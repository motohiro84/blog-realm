services:
  blog-public-client:
    build: .
    container_name: blog-public-client
    ports:
      - "3001:3000"
    networks:
      - blog-realm-network

networks:
  blog-realm-network:
    external: true
